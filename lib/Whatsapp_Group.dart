import 'package:flutter/material.dart';

class WhatsappGroup extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Whatsapp Group"),
      ),
      body: Center(
        child:Text('hi'),
      ),
    );
  }
}