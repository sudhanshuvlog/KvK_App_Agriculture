import 'package:flutter/material.dart';

class FarmerStory extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Farmer Story"),
      ),
      body: Center(
        child:Text('hi'),
      ),
    );
  }
}