import 'package:flutter/material.dart';

class Staf extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Staff"),
      ),
      body: Center(
        child:Text('hi'),
      ),
    );
  }
}