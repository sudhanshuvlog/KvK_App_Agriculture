import 'package:flutter/material.dart';

class DisplayUnit extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Display Unit"),
      ),
      body: Center(
        child:Text('hi'),
      ),
    );
  }
}